```shell
Y=2
x=$Y
X=pippo

echo $x
# visualizza il valore di x (2)

echo $PATH
# visualizza il valore della variabile PATH

PATH=$PATH:/usr/local/bin
# aggiunge la directory /usr/local/bin alle directory già
# contenute in PATH
```